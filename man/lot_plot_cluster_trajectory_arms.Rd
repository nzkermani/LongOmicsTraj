% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lot_plot_cluster_trajectory_arms.R
\name{lot_plot_cluster_trajectory_arms}
\alias{lot_plot_cluster_trajectory_arms}
\title{Plot trajectory arms within molecular-feature clusters}
\usage{
lot_plot_cluster_trajectory_arms(
  object,
  clusters = NULL,
  groups = NULL,
  top_n = NULL,
  min_proportion = 0,
  show_counts = TRUE,
  show_percent = TRUE,
  scale_width = TRUE,
  ncol = NULL
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object containing
`cluster_ots_distribution` results.}

\item{clusters}{Optional character vector of cluster identifiers to show.
By default, all clusters are included.}

\item{groups}{Optional character vector of clinical or treatment groups
to show. By default, all groups are included.}

\item{top_n}{Optional maximum number of trajectory arms displayed within
each cluster-group panel. By default, all trajectory classes are shown.}

\item{min_proportion}{Minimum within-cluster trajectory proportion required
for display. The default is `0`.}

\item{show_counts}{Logical; include feature counts in trajectory labels.}

\item{show_percent}{Logical; include percentages in trajectory labels.}

\item{scale_width}{Logical; scale arrow width by trajectory proportion.}

\item{ncol}{Number of facet columns.}
}
\value{
A `ggplot` object.
}
\description{
Visualises the distribution of feature-level Omics Trajectory
Signatures within each molecular-feature cluster. Each panel represents
one cluster and clinical group. Arrows emerge from a shared cluster node
and represent the distinct trajectory classes found inside that cluster.
}
\details{
Arrow width is proportional to the fraction of clustered features
assigned to each trajectory class.
}
\examples{
data("lot_glucold_ots")

example_object <- lot_glucold_ots

example_results <- lot_results(
  example_object
)

example_results$cluster_ots_distribution <-
  S4Vectors::DataFrame(
    cluster_id = c(
      "cluster_1",
      "cluster_1",
      "cluster_2",
      "cluster_2"
    ),
    group = "treated",
    topology_label = c(
      "up_down",
      "flat_flat",
      "down_up",
      "flat_down"
    ),
    count = c(
      8L,
      2L,
      7L,
      3L
    ),
    proportion = c(
      0.8,
      0.2,
      0.7,
      0.3
    )
  )

methods::slot(
  example_object,
  "results"
) <- example_results

plot <- lot_plot_cluster_trajectory_arms(
  object = example_object,
  groups = "treated"
)

plot
}
