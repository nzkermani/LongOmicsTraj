% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/consensus.R
\name{lot_consensus}
\alias{lot_consensus}
\alias{lot_consensus,DFrame-method}
\title{Calculate consensus trajectory assignments}
\usage{
lot_consensus(x, min_agreement = 2L, mode = c("strict", "majority"))

\S4method{lot_consensus}{DFrame}(x, min_agreement = 2L, mode = c("strict", "majority"))
}
\arguments{
\item{x}{A `DFrame` containing OTS assignments from multiple estimators.}

\item{min_agreement}{Minimum number of assignments that must agree before a
consensus topology is assigned.}

\item{mode}{Consensus rule. `"majority"` assigns the most frequent topology
when its count is at least `min_agreement`. `"strict"` assigns a topology
only when all available assignments agree and the number of assignments is
at least `min_agreement`.}
}
\value{
An object containing consensus topology assignments.

A `DFrame` containing one row per molecular feature with the
  consensus topology, agreement count and number of available assignments.
}
\description{
Combines Omics Trajectory Signature assignments from multiple estimators
into a consensus topology label.

Combines OTS assignments stored in a `DFrame` and returns a consensus
topology label for each molecular feature.
}
\details{
Multiple estimator and group-specific assignments are first collapsed to
one topology label per molecular feature, estimator and group. Consensus is
then calculated across the resulting assignments.
}
\examples{
assignments <- S4Vectors::DataFrame(
  object_id = rep(
    c("gene_1", "gene_2"),
    each = 3
  ),
  estimator = rep(
    c("empirical", "lmm", "gam"),
    times = 2
  ),
  group = "treated",
  topology_label = c(
    "up_down",
    "up_down",
    "flat_flat",
    "down_up",
    "down_up",
    "down_up"
  )
)

consensus <- lot_consensus(
  assignments,
  min_agreement = 2L,
  mode = "majority"
)

as.data.frame(consensus)

}
