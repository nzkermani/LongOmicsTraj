% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lot_plot_cluster_selection.R
\name{lot_plot_cluster_selection}
\alias{lot_plot_cluster_selection}
\title{Plot topology-aware cluster selection}
\usage{
lot_plot_cluster_selection(
  object,
  metrics = c("BIC", "weighted_purity", "passing_fraction", "minimum_cluster_size"),
  purity_threshold = 0.9,
  required_passing_fraction = 0.8,
  min_cluster_size = 5L,
  show_selected = TRUE
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object containing FlexMix model-comparison
results.}

\item{metrics}{Metrics to display. Supported values are `"BIC"`,
`"weighted_purity"`, `"passing_fraction"`,
`"minimum_cluster_size"`, `"minimum_purity"` and `"mean_purity"`.}

\item{purity_threshold}{Reference purity threshold.}

\item{required_passing_fraction}{Required fraction of clusters passing the
purity rule.}

\item{min_cluster_size}{Minimum acceptable cluster size.}

\item{show_selected}{Logical; mark the selected cluster resolution.}
}
\value{
A `ggplot` object.
}
\description{
Displays likelihood-based and topology-aware statistics across candidate
numbers of FlexMix clusters.
}
\examples{
data("lot_glucold_ots")

example_object <- lot_glucold_ots

example_results <- lot_results(
  example_object
)

example_results$flexmix_model_comparison <- S4Vectors::DataFrame(
  k = 2:4,
  BIC = c(540, 505, 512),
  weighted_purity = c(0.76, 0.91, 0.88),
  passing_fraction = c(0.50, 1.00, 0.75),
  minimum_cluster_size = c(18, 12, 6),
  minimum_purity = c(0.68, 0.87, 0.72),
  mean_purity = c(0.78, 0.92, 0.86)
)

example_results$flexmix_selected_k <- 3L

methods::slot(
  example_object,
  "results"
) <- example_results

plot <- lot_plot_cluster_selection(
  object = example_object
)

plot
}
