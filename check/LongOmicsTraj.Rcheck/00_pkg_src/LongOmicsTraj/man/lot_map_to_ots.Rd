% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/lot_map_to_ots.R
\name{lot_map_to_ots}
\alias{lot_map_to_ots}
\alias{lot_map_to_ots,LongOmicsTraj-method}
\title{Map molecular changes to Omics Trajectory Signatures}
\usage{
lot_map_to_ots(object, visits, overwrite = FALSE, ...)

\S4method{lot_map_to_ots}{LongOmicsTraj}(object, visits, overwrite = FALSE, ...)
}
\arguments{
\item{object}{A `LongOmicsTraj` object.}

\item{visits}{Character vector giving the visits in chronological order.}

\item{overwrite}{Logical; overwrite existing OTS results when `TRUE`.}

\item{...}{Additional arguments reserved for future extensions.}
}
\value{
A `LongOmicsTraj` object containing updated OTS assignments.

A `LongOmicsTraj` object with updated OTS assignments.
}
\description{
Classifies molecular changes between consecutive visits and combines them
into feature-level Omics Trajectory Signature labels.

Classifies feature-level molecular changes between consecutive visits and
combines the transition states into Omics Trajectory Signature labels.
}
\details{
The method uses delta estimates and thresholds already stored in the
`LongOmicsTraj` object.
}
\examples{
data("lot_glucold_ots")

visits <- c(
  "Baseline",
  "6 Months",
  "30 Months"
)

fitted <- lot_map_to_ots(
  object = lot_glucold_ots,
  visits = visits,
  overwrite = TRUE
)

utils::head(
  as.data.frame(
    lot_ots(fitted)
  )
)
}
