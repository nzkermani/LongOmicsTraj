% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/query.R
\name{lot_query}
\alias{lot_query}
\alias{NOT}
\alias{lot_query,LongOmicsTraj-method}
\title{Query LongOmicsTraj results}
\usage{
lot_query(object, ...)

NOT(x)

\S4method{lot_query}{LongOmicsTraj}(
  object,
  object_type = NULL,
  assay = NULL,
  group = NULL,
  topology = NULL,
  conditions = NULL,
  estimator = NULL
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object.}

\item{...}{Query arguments passed to the corresponding method.}

\item{x}{Character vector of topology labels to exclude.}

\item{object_type}{Optional character vector of molecular object types.}

\item{assay}{Optional character vector of assays.}

\item{group}{Optional character vector of clinical or treatment groups.}

\item{topology}{Optional character vector of OTS topology labels.}

\item{conditions}{Optional named list defining group-specific topology
conditions. Each element may be a character vector, a `NOT()` condition,
or a list with `include` and/or `exclude` elements.}

\item{estimator}{Optional character vector of trajectory estimators.}
}
\value{
A filtered result object.

An object of class `"lot_not"` representing an exclusion condition.

A `DataFrame` containing the filtered OTS assignments.
}
\description{
Retrieves molecular features or trajectory results matching user-defined
criteria.

Creates an exclusion condition for use in `lot_query()`. Features whose
topology labels match any value in `x` are excluded.

Filters Omics Trajectory Signature assignments stored in a
`LongOmicsTraj` object.
}
\details{
Queries may be restricted by molecular object type, assay, group,
estimator or topology label. The `conditions` argument supports
group-specific cross-arm queries.


Simple filters are applied directly to the OTS table. Cross-group queries
supplied through `conditions` require the same molecular feature to satisfy
all named group-specific conditions.
}
\examples{
data("lot_glucold_ots")

flat <- lot_query(
  object = lot_glucold_ots,
  topology = "flat_flat"
)

nrow(flat)

utils::head(
  as.data.frame(flat)
)
}
