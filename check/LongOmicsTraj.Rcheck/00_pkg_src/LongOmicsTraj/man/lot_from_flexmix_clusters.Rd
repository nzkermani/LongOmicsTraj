% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lot_from_flexmix_clusters.R
\name{lot_from_flexmix_clusters}
\alias{lot_from_flexmix_clusters}
\title{Cluster molecular features using FlexMix trajectory models}
\usage{
lot_from_flexmix_clusters(
  object,
  assay,
  group,
  visits,
  k = 2:5,
  assay_data = NULL,
  group_col = "group",
  visit_col = "visit",
  sample_col = "sample_id",
  trajectory_source = c("estimated", "mean"),
  trajectory_estimator = NULL,
  model = c("quadratic", "linear"),
  scale_features = TRUE,
  purity_threshold = 0.9,
  required_passing_fraction = 0.8,
  min_cluster_size = 5L,
  nrep = 10L,
  seed = 42L,
  store_fits = FALSE,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object containing molecular data and
feature-level OTS results.}

\item{assay}{Name of the experiment in the `MultiAssayExperiment`.}

\item{group}{Clinical or treatment group to analyse.}

\item{visits}{Character vector giving the chronological visit order.}

\item{k}{Integer vector containing candidate numbers of clusters.}

\item{assay_data}{Optional assay-matrix name. The first available matrix is
used by default.}

\item{group_col}{Metadata column containing clinical groups.}

\item{visit_col}{Metadata column containing visits.}

\item{sample_col}{Metadata column containing sample identifiers.}

\item{trajectory_source}{Source of the gene trajectories: `"estimated"`
uses `object@vmm`, while `"mean"` calculates visit-level assay means.}

\item{trajectory_estimator}{Optional estimator used to select trajectories
from `object@vmm`, such as `"masigpro"`, `"gam"`, `"lmm"` or
`"empirical"`.}

\item{model}{Trajectory regression model: `"quadratic"` or `"linear"`.}

\item{scale_features}{Logical; standardise each molecular-feature trajectory
before clustering.}

\item{purity_threshold}{Minimum dominant-OTS proportion required for a
cluster to pass the topology-purity criterion.}

\item{required_passing_fraction}{Minimum fraction of clusters required to
pass the topology-purity criterion.}

\item{min_cluster_size}{Minimum allowed number of molecular features in a
cluster.}

\item{nrep}{Number of repeated FlexMix initialisations for each candidate
value of `k`.}

\item{seed}{Random seed.}

\item{store_fits}{Logical; store the fitted FlexMix models in
`object@results`.}

\item{overwrite}{Logical; overwrite existing FlexMix clustering results.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A `LongOmicsTraj` object containing model-comparison statistics,
  the selected cluster resolution, feature assignments, cluster
  trajectories, OTS distributions and topology-purity summaries.
}
\description{
Fits finite mixtures of longitudinal regression trajectories to molecular
features within one clinical or treatment group. Candidate cluster
resolutions are evaluated using likelihood-based model statistics and
LongOmicsTraj topology purity.
}
\details{
Trajectories can be obtained from model-estimated values already stored in
`object@vmm`, or calculated as mean molecular values at each visit from the
original assay matrix.


The selected solution is the smallest candidate `k` for which:

\itemize{
  \item the required fraction of clusters reaches `purity_threshold`; and
  \item all clusters contain at least `min_cluster_size` features.
}

If no candidate satisfies both criteria, the solution with the lowest BIC
is selected.

When `trajectory_source = "estimated"`, LongOmicsTraj trajectories stored
in `object@vmm` are used. This is generally preferred because trajectories
may already account for repeated measurements, smoothing or other model
assumptions.
}
\examples{
\dontrun{
data("lot_glucold_ots")

lot_flexmix <- lot_from_flexmix_clusters(
  object = lot_glucold_ots,
  assay = "transcriptomics",
  group = "ICS 6 months then withdrawal",
  visits = c(
    "Baseline",
    "6 Months",
    "30 Months"
  ),
  trajectory_source = "estimated",
  trajectory_estimator = "masigpro",
  k = 2:5,
  purity_threshold = 0.90,
  required_passing_fraction = 0.80,
  min_cluster_size = 5
)
}
}
