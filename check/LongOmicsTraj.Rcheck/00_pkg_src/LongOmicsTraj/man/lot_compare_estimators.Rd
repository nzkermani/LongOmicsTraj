% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/compare-estimators.R
\name{lot_compare_estimators}
\alias{lot_compare_estimators}
\alias{lot_compare_estimators,LongOmicsTraj-method}
\title{Compare trajectory estimators}
\usage{
lot_compare_estimators(object, ..., .names = NULL)

\S4method{lot_compare_estimators}{LongOmicsTraj}(object, ..., .names = NULL)
}
\arguments{
\item{object}{A `LongOmicsTraj` object containing OTS assignments.}

\item{...}{Additional `LongOmicsTraj` objects containing OTS assignments.}

\item{.names}{Optional character vector giving estimator labels. When
omitted, labels of the form `"estimator_1"`, `"estimator_2"` and so on
are used.}
}
\value{
An `S4Vectors::DataFrame` containing the combined OTS assignments.

An `S4Vectors::DataFrame` containing the combined OTS assignments
  and an `estimator` column identifying their source.
}
\description{
Combines Omics Trajectory Signature assignments generated by two or more
trajectory estimators into a common comparison table.

Combines Omics Trajectory Signature assignments from two or more
`LongOmicsTraj` objects into a common table suitable for agreement and
consensus analyses.
}
\details{
Each supplied object must contain OTS assignments generated by
`lot_map_to_ots()`. The original estimator column is replaced by the labels
supplied through `.names`, allowing results from different modelling
strategies to be compared directly.
}
\examples{
data("lot_glucold_ots")

## For illustration, compare two stored trajectory analyses.
comparison <- lot_compare_estimators(
  lot_glucold_ots,
  lot_glucold_ots,
  .names = c(
    "analysis_1",
    "analysis_2"
  )
)

comparison <- as.data.frame(comparison)

table(
  comparison$estimator
)

utils::head(
  comparison[
    ,
    c(
      "object_id",
      "group",
      "topology_label",
      "estimator"
    )
  ]
)
}
