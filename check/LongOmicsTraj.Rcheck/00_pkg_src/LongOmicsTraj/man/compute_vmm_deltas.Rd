% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_vmm_deltas.R
\name{compute_vmm_deltas}
\alias{compute_vmm_deltas}
\title{Compute adjacent visit-to-visit deltas from a Visit Mean Matrix}
\usage{
compute_vmm_deltas(vmm, visits)
}
\arguments{
\item{vmm}{A data.frame with columns object_id, object_type, group, visit,
estimated_value, estimator.}

\item{visits}{Character vector giving the ordered visits.}
}
\value{
A data.frame of adjacent deltas.
}
\description{
Compute adjacent visit-to-visit deltas from a Visit Mean Matrix
}
\examples{
visits <- c(
  "Baseline",
  "Week 4",
  "Week 12"
)

vmm <- data.frame(
  object_id = rep(
    c("gene_1", "gene_2"),
    each = 3
  ),
  object_type = "gene",
  group = "treated",
  visit = rep(
    visits,
    times = 2
  ),
  estimated_value = c(
    1.0, 1.6, 1.2,
    2.0, 2.0, 2.5
  ),
  estimator = "empirical",
  stringsAsFactors = FALSE
)

deltas <- compute_vmm_deltas(
  vmm = vmm,
  visits = visits
)

deltas[
  ,
  c(
    "object_id",
    "transition",
    "delta_hat"
  )
]

}
