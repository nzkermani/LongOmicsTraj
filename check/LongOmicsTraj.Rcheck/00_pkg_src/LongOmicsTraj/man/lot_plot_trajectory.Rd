% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lot_plot_trajectory.R
\name{lot_plot_trajectory}
\alias{lot_plot_trajectory}
\title{Plot longitudinal trajectories for selected molecular features}
\usage{
lot_plot_trajectory(
  object,
  features,
  assay = "transcriptomics",
  assay_data = NULL,
  groups = NULL,
  estimator = NULL,
  show_subjects = FALSE,
  summary = c("mean_se", "mean_ci", "mean"),
  facet_scales = c("free_y", "fixed"),
  ncol = NULL
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object.}

\item{features}{Character vector of feature identifiers, such as gene symbols.}

\item{assay}{Name of the experiment in the `MultiAssayExperiment`.}

\item{assay_data}{Optional name of the matrix within the
`SummarizedExperiment`. By default, the first available matrix is used.}

\item{groups}{Optional character vector of groups to display. By default,
all available groups are included.}

\item{estimator}{Optional estimator used to retrieve OTS labels.}

\item{show_subjects}{Logical; if `TRUE`, individual-subject trajectories
are displayed as faint lines.}

\item{summary}{One of `"mean_se"`, `"mean_ci"` or `"mean"`.}

\item{facet_scales}{Either `"free_y"` or `"fixed"`.}

\item{ncol}{Number of facet columns.}
}
\value{
A `ggplot` object.
}
\description{
Plots observed longitudinal measurements for one or more molecular
features across clinical groups. Individual-subject profiles, group
means, uncertainty intervals and assigned Omics Trajectory Signatures
can be displayed.
}
\examples{
data("lot_glucold_ots")

lot_plot_trajectory(
  lot_glucold_ots,
  features = c("ATP2A3", "CAT"),
  groups = c(
    "ICS 30 months",
    "ICS 6 months then withdrawal",
    "Placebo"
  ),
  estimator = "masigpro",
  show_subjects = TRUE
)
}
