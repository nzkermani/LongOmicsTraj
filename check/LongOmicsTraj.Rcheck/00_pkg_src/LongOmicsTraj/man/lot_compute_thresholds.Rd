% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/thresholds.R
\name{lot_compute_thresholds}
\alias{lot_compute_thresholds}
\alias{lot_compute_thresholds,LongOmicsTraj-method}
\title{Compute molecular-change thresholds}
\usage{
lot_compute_thresholds(object, ...)

\S4method{lot_compute_thresholds}{LongOmicsTraj}(
  object,
  assay,
  method = c("fixed", "global_mad", "gene_mad", "hybrid", "quantile"),
  delta_min = NULL,
  k = 0.25,
  prob = 0.75,
  visits,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object.}

\item{...}{Additional arguments passed to the corresponding method.}

\item{assay}{Name of the assay for which thresholds should be computed.}

\item{method}{Threshold-estimation method. One of `"fixed"`,
`"global_mad"`, `"gene_mad"`, `"hybrid"` or `"quantile"`.}

\item{delta_min}{Optional minimum threshold value. When `NULL`, the median
absolute delta across all objects and transitions is used.}

\item{k}{Numeric scaling factor applied to MAD-based thresholds.}

\item{prob}{Probability used for quantile-based threshold estimation.}

\item{visits}{Character vector giving the visits in chronological order.
When omitted, `object@settings$visits` is used.}

\item{overwrite}{Logical; overwrite existing thresholds for the selected
assay when `TRUE`.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A `LongOmicsTraj` object containing updated threshold results.

A `LongOmicsTraj` object with updated threshold results.
}
\description{
Computes thresholds used to classify molecular changes between consecutive
visits.

Computes feature-specific thresholds used to classify molecular changes
between consecutive visits as down, flat or up.
}
\details{
Thresholds are calculated from the delta estimates already stored in the
`LongOmicsTraj` object. The function supports fixed, global MAD,
feature-specific MAD, hybrid and quantile-based thresholding.


Delta values are grouped by molecular object and converted into a matrix
of adjacent-visit changes. The available thresholding methods are:

\describe{
  \item{`fixed`}{
    Uses the same `delta_min` threshold for every object.
  }
  \item{`global_mad`}{
    Uses a single threshold based on the global median MAD.
  }
  \item{`gene_mad`}{
    Uses an object-specific MAD threshold.
  }
  \item{`hybrid`}{
    Uses the maximum of `delta_min` and the object-specific MAD threshold.
  }
  \item{`quantile`}{
    Uses the maximum of `delta_min` and an object-specific absolute-delta
    quantile.
  }
}

The function supports both gene-level and cluster-level delta results.
Object type is inferred from identifiers beginning with `"cluster_"`.
}
\examples{
data("lot_glucold_ots")

visits <- c(
  "Baseline",
  "6 Months",
  "30 Months"
)

fitted <- lot_compute_thresholds(
  object = lot_glucold_ots,
  assay = "transcriptomics",
  method = "hybrid",
  visits = visits,
  overwrite = TRUE
)

thresholds <- as.data.frame(
  lot_thresholds(fitted)
)

utils::head(
  thresholds[
    ,
    c(
      "object_id",
      "delta_g",
      "threshold_method"
    )
  ]
)
}
