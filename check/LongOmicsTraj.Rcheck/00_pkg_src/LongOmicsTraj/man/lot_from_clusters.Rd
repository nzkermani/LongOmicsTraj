% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllGenerics.R, R/lot_from_clusters.R
\name{lot_from_clusters}
\alias{lot_from_clusters}
\alias{lot_from_clusters,LongOmicsTraj-method}
\title{Cluster molecular features by longitudinal trajectory}
\usage{
lot_from_clusters(object, ...)

\S4method{lot_from_clusters}{LongOmicsTraj}(
  object,
  assay,
  group_col = "group",
  visit_col = "visit",
  visits,
  k = 4L,
  estimator = c("kmeans", "pam"),
  assay_data = NULL,
  scale_features = TRUE,
  standardise_ids = FALSE,
  seed = 1L,
  require_gene_ots = FALSE,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object.}

\item{...}{Additional clustering arguments passed to the method.}

\item{assay}{Name of the assay to analyse.}

\item{group_col}{Metadata column defining clinical or treatment groups.}

\item{visit_col}{Metadata column defining visits.}

\item{visits}{Character vector giving the chronological visit order.}

\item{k}{Number of clusters.}

\item{estimator}{Clustering method: `"kmeans"` or `"pam"`.}

\item{assay_data}{Optional assay-matrix name. The first available matrix
is used by default.}

\item{scale_features}{Logical; standardise each feature trajectory before
clustering. The default is `TRUE`.}

\item{standardise_ids}{Logical; convert feature identifiers to uppercase
before clustering and matching OTS results. The default is `FALSE`.}

\item{seed}{Random seed used by k-means.}

\item{require_gene_ots}{Logical; require existing feature-level OTS results.}

\item{overwrite}{Logical; overwrite an existing visit-level model.}

\item{verbose}{Logical; print progress messages.}
}
\value{
A `LongOmicsTraj` object containing cluster-level trajectories
and feature-to-cluster relationships.

A `LongOmicsTraj` object containing cluster-centre trajectories,
  feature-to-cluster relationships, raw and scaled cluster profiles,
  clustering diagnostics and, when available, feature-level OTS
  distributions within clusters.
}
\description{
Groups molecular features according to similarities in their longitudinal
profiles.

Clusters molecular features using their combined group-by-visit
longitudinal profiles. Cluster centres are stored as visit-level
molecular models, while feature-to-cluster memberships and clustering
diagnostics are stored in the results and relationships slots.
}
\details{
When feature-level OTS results already exist, they are preserved and
summarised within each trajectory cluster.


Each molecular feature is represented by a vector containing its mean
value for every observed group-by-visit combination. Clustering is
therefore based on the feature's combined longitudinal behaviour across
all included groups.

When `scale_features = TRUE`, clustering and cluster-level OTS mapping
use standardised feature profiles. Both raw and scaled cluster centres
are retained in `object@results$cluster_profiles`.
}
