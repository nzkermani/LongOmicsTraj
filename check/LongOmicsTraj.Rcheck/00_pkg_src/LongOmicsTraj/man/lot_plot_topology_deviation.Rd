% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lot_plot_topology_deviation.R
\name{lot_plot_topology_deviation}
\alias{lot_plot_topology_deviation}
\title{Plot topology deviations within clusters}
\usage{
lot_plot_topology_deviation(
  object,
  clusters = NULL,
  group = NULL,
  min_abs_deviation = 0,
  top_n = NULL,
  sort_topologies = TRUE
)
}
\arguments{
\item{object}{A `LongOmicsTraj` object containing selected FlexMix
clustering results.}

\item{clusters}{Optional vector of clusters to show.}

\item{group}{Optional clinical group.}

\item{min_abs_deviation}{Minimum absolute deviation required for display.}

\item{top_n}{Optional maximum number of topologies displayed per cluster.}

\item{sort_topologies}{Logical; order topologies by deviation within each
cluster.}
}
\value{
A `ggplot` object.
}
\description{
Creates a PAMR-inspired nail plot showing how strongly each Omics
Trajectory Signature is enriched or depleted within each cluster compared
with its overall frequency.
}
\details{
For topology `t` in cluster `c`, deviation is calculated as:

\deqn{
D_{ct} = p_{ct} - p_t
}

where `p_ct` is the within-cluster topology proportion and `p_t` is the
overall topology proportion.
}
\examples{
data("lot_glucold_ots")

example_object <- lot_glucold_ots

example_results <- lot_results(
  example_object
)

example_results$flexmix_ots_distribution <-
  S4Vectors::DataFrame(
    cluster = c(
      "1",
      "1",
      "2",
      "2"
    ),
    group = "treated",
    topology_label = c(
      "up_down",
      "flat_flat",
      "up_down",
      "flat_flat"
    ),
    n_genes = c(
      8L,
      2L,
      3L,
      7L
    ),
    proportion = c(
      0.8,
      0.2,
      0.3,
      0.7
    )
  )

methods::slot(
  example_object,
  "results"
) <- example_results

plot <- lot_plot_topology_deviation(
  object = example_object,
  group = "treated"
)

plot
}
